using System.Collections.ObjectModel;

namespace MyFoodApp.Services.ApiToFoodFactory
{
/*
    internal static class FoodItemFactory
    {
        public static ObservableCollection<RecipeDataModel> GetFoodItems(FoodData foodData)
        {
            var foodItems = new ObservableCollection<RecipeDataModel>();
            foreach (var fData in foodData.Hits)
                foodItems.Add(fData.RecipeDataModel);
            return foodItems;
        }
    }
*/
}